<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'active-rooms' %>
      <% end %>
      <%= render 'feed' %>
      <%= render 'topics' %>

    </div>
  </div>
</section>
